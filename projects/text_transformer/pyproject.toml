[build-system]
requires = ["hatchling", "hatch-polylith-bricks"]
build-backend = "hatchling.build"

[project]
name = "text-transformer"
version = "0.1.0"
description = "Basic text transformation operations - format, case, trimming"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "typer>=0.16.1",
    "rich>=14.1.0",
    "pyperclip>=1.9.0",
    "structlog>=25.4.0",
    "chardet>=5.2.0",
]

[project.scripts]
tt = "text_transformer.cli:app"
text-transformer = "text_transformer.cli:app"

# カスタムトップネームスペース設定
[tool.polylith.build]
top-namespace = "textkit"

[tool.polylith.bricks]
"../../components/text_core" = "text_processing/text_core"
"../../components/config_manager" = "text_processing/config_manager"
"../../components/io_handler" = "text_processing/io_handler"
"../../components/rule_parser" = "text_processing/rule_parser"
"../../components/exceptions" = "text_processing/exceptions"
"../../components/common_utils" = "common_utils"

[tool.hatch.build.hooks.polylith-bricks]

[tool.hatch.metadata]
allow-direct-references = true
